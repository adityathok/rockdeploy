{
  "name": "rockdeploy",
  "version": "1.0.0",
  "description": "Tool Deploy Project Rock",
  "main": "deploy.js",
  "scripts": {
    "deploy": "node deploy.js",
    "deploy:clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "cross-env": "^10.1.0",
    "ftp-deploy": "^2.4.7"
  }
}
